// 機材シート2026 モバイル用のサービスワーカー
// アプリの外枠（HTML/CSS/JS/アイコン）だけをキャッシュしておき、
// 電波が弱い場所でも起動できるようにする。
// データそのもの（予約一覧など）は毎回ネットワークから取得する
// （キャッシュを見せてしまうと古いデータに気づけなくなるため）。

// 【2026/8/29修正】アイコン画像（icon-192.png/icon-512.png）を透明背景から白背景に
// 差し替えた（iOSのホーム画面アイコンは透明// 機材シート2026 モバイル用のサービスワーカー
// アプリの外枠（HTML/CSS/JS/アイコン）だけをキャッシュしておき、
// 電波が弱い場所でも起動できるようにする。
// データそのもの（予約一覧など）は毎回ネットワークから取得する
// （キャッシュを見せてしまうと古いデータに気づけなくな// 機材シート// 機材シート2026 モバイル用のサービスワーカー
// アプリの外枠（HTML/CSS/JS/アイコン）だけをキャッシュしておき、
// 電波が弱い場所でも起動できるようにする。
// データそのもの（予約一覧など）は毎回ネットワークから取得する
// （キャッシュを見せてしまうと古いデータに気づけなくなるため）。// 機材シート2026 モバイル用のサービスワーカー
// アプリの外枠（HTML/CSS/JS/アイコン）だけをキャッシュしておき、
// 電波が弱い場所でも起動できるようにする。
// データそのもの（予約一覧など）は毎回ネットワークから取得する
// （キャッシュを見せてしまうと古いデータに気づけなくなるため）。

// 【2026/8/29修正】アイコン画像（icon-192.png/icon-512.png）を透明背景から白背景に
// 差し替えた（iOSのホーム画面アイコンは透明部分を黒く塗りつぶして表示してしまうため、
// 「渡したアイコンと違って見える」原因になっていた）。古いキャッシュに透明版の
// アイコンが残っている端末でも、確実に新しい画像を取りに行けるようキャッシュ名を
// 更新した。
// 【2026/9/3追加】ホーム画面に追加したスマホ（特にiPhone）で、機材のカテゴリー分け
// 機能を追加したあとも古い画面のまま変わらないという報告があった。index.html自体は
// 毎回ネットワークから取りに行く設計（下のfetchハンドラ参照）なので通常は問題ない
// はずだが、機種によってはservice worker（このファイル自体）の更新チェックが
// すぐには走らず、古いservice workerが動き続けてしまうことがある。合言葉の
// 再ログインではこの層のキャッシュは消えない（ログイン情報とservice workerの
// キャッシュは別物のため）。キャッシュ名を上げておくことで、次に新しい
// service workerへの切り替えが行われたタイミングで、確実に前のキャッシュ
// （kizai-mobile-v7以前）が破棄されるようにした。
// 【2026/9/12追加】アプリ内「ガントチャート」タブを追加（index.html更新）。
// 念のため、こちらもキャッシュ名を上げておく。
// 【2026/9/13追加】ガントチャートタブが表示されない不具合の調査・修正のため
// index.htmlを更新。あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その2】ガントチャートの見た目を修正（機材名の欄を左右スクロール
// 対応にし、上部の凡例を機材カテゴリーのショートカットボタンに差し替え）。
// index.htmlを更新したため、あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その3】機材名欄の横スクロールを全行連動にした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加】機材名欄の連動スクロールが実機で効かない不具合を修正。
// ブラウザ標準の横スクロールに頼らず、指のドラッグを直接読み取る方式に作り替えた
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その2】機材名欄のスワイプが重いという指摘に対応。ドラッグ中に
// 毎回文字の幅を測り直していたのをやめ、描画時に1回だけ測ってキャッシュし、画面への
// 反映もrequestAnimationFrameでまとめるようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その3】機材名が短い行だけ途中で止まって見える動きは不要とのことで、
// 全行が常に同じ量だけ動くように変更（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/15追加】指が機材名欄からガントチャート本体側にはみ出すと途中でスライドが
// 止まってしまう不具合を修正。タッチ操作は生のTouchイベントで直接扱い、指の位置に
// 関わらず同じ指である限りスライドを継続するようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/16追加】機材名欄で指を上下に動かしたときは、ガントチャート全体を上下に
// 見られるよう、標準の縦スクロールに任せるようにした（横方向のスライドとどちらの
// 操作か、最初の指の動きで判定する）（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その2】カレンダーの日付が見づらいという指摘に対応し、数字を
// はっきり黒くした。ガントチャートの日付も、1つ1つ枠で囲んで見やすくした
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その3】日付の枠をヘッダーだけでなく、ガントチャート本体（予約の帯が
// 並ぶ部分）にも入れ、縦の区切り線でどの日付か分かりやすくした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その4】予約の帯にカーソルを合わせたときの詳細表示を、ブラウザ標準
// のtitle属性（3秒程度）から、自前の吹き出し（1秒）に変更した（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/17追加】(1) PCのブラウザでは機材名欄をマウスでドラッグしても文字列選択が
// 先に発生してしまい横スライドが効かない不具合を修正（文字選択を禁止し、
// pointerdownでpreventDefault）。(2) ガントチャートの月移動を「前」「次」ボタン
// （30日ずつしか進めない）から、期間表示をタップして直接月を選べる方式に変更した
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/17追加 その2】タップしないと選べる月が見えないピッカー方式をやめ、
// 月のボタンを最初から横一列に並べて表示するようにした。入りきらない月は
// この行自体を指でスライドして見られる（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/18追加】ガントチャート本体を「次」「今月を選ぶ」ボタンを押さずに
// 指でスライドし続けるだけで、もっと先・もっと前の日付まで途中で止まらずに
// 見られるようにした（端に近づくたびに実際の表示日数を延長し、見た目の位置が
// ずれないようスクロール位置も補正する。上限は約3年）（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/18追加 その2】(1) スマホ・タブレットを横向きにしたときに左右の余白が
// 大きすぎた点を修正（横向きのときだけ画面の横幅をもっと使うようにした）。
// (2) 横向きのときはガントチャートの縦の表示範囲がとても狭くなっていた点を修正
// （ヘッダーやボタン類の余白を詰め、期間表示と月ボタンの行を1行にまとめ、
// 表本体の高さを実際の画面サイズから計算し直すようにした）。
// (3) あわせて、初回表示のたびに意図せず過去方向へ余分に延長されてしまっていた
// 不具合（(1)(2)の確認中に発覚）も修正した（index.html更新）。あわせて
// キャッシュ名も上げる。
// 【2026/9/18追加 その3】横向き＋ガントチャート表示のときは、これまでロゴ付き
// ヘッダー分だけ表の行が少なく見えていた（下に1回スクロールしてヘッダーが
// 隠れると、その分多く見えるようになっていた）。毎回手でスクロールしなくても
// 最初からその「隠れた後」の表示になるよう、この組み合わせのときだけヘッダーを
// 表示しないようにした（他のタブに切り替えれば今まで通り表示される）
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/18追加 その4】横向きにしたとき、実機で「画面の一番下に背景の写真が
// 少し見えてしまう（表本体の高さが実際より低く見積もられている）」「画面の
// 横幅を最後まで使い切れていない」という報告があったため再修正。(1) 表本体の
// 高さの計算に、ブラウザのアドレスバーの出入りをより正確に捉えられる
// window.visualViewportを使うようにし、タブ切り替え・画面回転のあとも
// 時間差で何度か測り直すようにした（アドレスバーの動きが収まるまで待つため）。
// あわせて、ホームインジケーターの分はenv(safe-area-inset-bottom)としてCSS側で
// 確保するようにした。(2) 横幅の上限（1100px）を撤廃し、画面幅いっぱいまで
// 使うようにした。あわせて、ノッチが左右に来る端末向けに、その分の
// safe-area-inset-left/rightもpaddingとして確保した（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/19追加】前回(v26)の横向き向け修正(幅の上限撤廃・visualViewport/
// safe-area-inset-bottomを使った高さ計算)が、実機ではかえって左右の余白・
// 画面下部の余白を増やす結果になってしまったとの報告があったため、その2点は
// 前々回(v25)の状態に戻した(幅は再び1100pxを上限に、高さはwindow.innerHeight
// だけを使うシンプルな計算に戻し、下に残す余白も10px→4pxに減らした)。
// あわせて、ガントチャートの日付ヘッダー行の高さを機材の行の半分にし、
// その分だけ機材の行がもう1行分見えるようにした(index.html更新)。
// あわせてキャッシュ名も上げる。
// 【2026/9/19追加 その2】画面をスクロールしてスマホのアドレスバーが引っ込んだ
// あとに、ガントチャート表本体の下に余白が残ってしまう不具合がまだ直って
// いなかった。原因は、高さの計算にwindow.innerHeightという「その瞬間だけの
// 値」を使っていたため、あとからアドレスバーが引っ込んで画面が広がっても
// 反映されないことがあった点。CSSの動的ビューポート単位（100dvh）に
// 計算そのものを任せる方式に変更した。100dvhはブラウザ自身がアドレスバーの
// 出入りに常に追従してくれる値のため、JSで再計算しなくても、あとから画面が
// 広がれば自動的に高さも広がるようになる（index.html更新）。
// あわせてキャッシュ名も上げる。
const CACHE_NAME = 'kizai-mobile-v28';
const APP_SHELL = [
  './',
  './index.html',
  './manifest.json',
  './icon-192.png',
  './icon-512.png'
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => cache.addAll(APP_SHELL))
  );
  self.skipWaiting();
});

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((names) => {
      return Promise.all(
        names.filter((name) => name !== CACHE_NAME).map((name) => caches.delete(name))
      );
    })
  );
  self.clients.claim();
});

self.addEventListener('fetch', (event) => {
  const url = event.request.url;

  // Apps ScriptのWeb App（データの取得・保存）は常にネットワークを優先する
  if (url.indexOf('script.google.com') !== -1 || url.indexOf('script.googleusercontent.com') !== -1) {
    event.respondWith(
      fetch(event.request).catch(() => new Response(
        JSON.stringify({ ok: false, error: 'オフラインのため通信できません' }),
        { headers: { 'Content-Type': 'application/json' } }
      ))
    );
    return;
  }

  // ページ本体（index.html）は「まずネットワークから最新版を取りに行き、取れなければキャッシュ」にする
  // （こうしないと、コード更新後も古い画面がキャッシュから表示され続けてしまうため）
  // 【2026/8/28修正】fetch()に何も指定しないと、ブラウザ自身が持っているHTTPキャッシュ
  // （Service WorkerのCache APIとは別物）がまだ「新しい」と判断されている間は、
  // ネットワークに問い合わせすら行かずそのキャッシュがそのまま返ってきてしまう
  // ことが分かった。GitHub Pagesは配信するファイルに数分程度のキャッシュ有効期限を
  // 付けているため、更新後しばらくは（ブラウザを開き直しても）以前の内容が
  // 表示され続けてしまっていた。{cache: 'no-store'}を指定し、常にネットワークへ
  // 直接問い合わせて本当に最新の内容を取りに行くようにした。
  if (event.request.mode === 'navigate' || url.indexOf('index.html') !== -1) {
    event.respondWith(
      fetch(event.request, { cache: 'no-store' })
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => caches.match(event.request))
    );
    return;
  }

  // アイコンやmanifestなど、めったに変わらないファイルは「キャッシュがあればまずそれを返しつつ、裏で最新版に更新」する
  event.respondWith(
    caches.match(event.request).then((cached) => {
      const fetchPromise = fetch(event.request)
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => cached);
      return cached || fetchPromise;
    })
  );
});


// 【2026/8/29修正】アイコン画像（icon-192.png/icon-512.png）を透明背景から白背景に
// 差し替えた（iOSのホーム画面アイコンは透明部分を黒く塗りつぶして表示してしまうため、
// 「渡したアイコンと違って見える」原因になっていた）。古いキャッシュに透明版の
// アイコンが残っている端末でも、確実に新しい画像を取りに行けるようキャッシュ名を
// 更新した。
// 【2026/9/3追加】ホーム画面に追加したスマホ（特にiPhone）で、機材のカテゴリー分け
// 機能を追加したあとも古い画面のまま変わらないという報告があった。index.html自体は
// 毎回ネットワークから取りに行く設計（下のfetchハンドラ参照）なので通常は問題ない
// はずだが、機種によってはservice worker（このファイル自体）の更新チェックが
// すぐには走らず、古いservice workerが動き続けてしまうことがある。合言葉の
// 再ログインではこの層のキャッシュは消えない（ログイン情報とservice workerの
// キャッシュは別物のため）。キャッシュ名を上げておくことで、次に新しい
// service workerへの切り替えが行われたタイミングで、確実に前のキャッシュ
// （kizai-mobile-v7以前）が破棄されるようにした。
// 【2026/9/12追加】アプリ内「ガントチャート」タブを追加（index.html更新）。
// 念のため、こちらもキャッシュ名を上げておく。
// 【2026/9/13追加】ガントチャートタブが表示されない不具合の調査・修正のため
// index.htmlを更新。あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その2】ガントチャートの見た目を修正（機材名の欄を左右スクロール
// 対応にし、上部の凡例を機材カテゴリーのショートカットボタンに差し替え）。
// index.htmlを更新したため、あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その3】機材名欄の横スクロールを全行連動にした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加】機材名欄の連動スクロールが実機で効かない不具合を修正。
// ブラウザ標準の横スクロールに頼らず、指のドラッグを直接読み取る方式に作り替えた
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その2】機材名欄のスワイプが重いという指摘に対応。ドラッグ中に
// 毎回文字の幅を測り直していたのをやめ、描画時に1回だけ測ってキャッシュし、画面への
// 反映もrequestAnimationFrameでまとめるようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その3】機材名が短い行だけ途中で止まって見える動きは不要とのことで、
// 全行が常に同じ量だけ動くように変更（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/15追加】指が機材名欄からガントチャート本体側にはみ出すと途中でスライドが
// 止まってしまう不具合を修正。タッチ操作は生のTouchイベントで直接扱い、指の位置に
// 関わらず同じ指である限りスライドを継続するようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/16追加】機材名欄で指を上下に動かしたときは、ガントチャート全体を上下に
// 見られるよう、標準の縦スクロールに任せるようにした（横方向のスライドとどちらの
// 操作か、最初の指の動きで判定する）（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その2】カレンダーの日付が見づらいという指摘に対応し、数字を
// はっきり黒くした。ガントチャートの日付も、1つ1つ枠で囲んで見やすくした
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その3】日付の枠をヘッダーだけでなく、ガントチャート本体（予約の帯が
// 並ぶ部分）にも入れ、縦の区切り線でどの日付か分かりやすくした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その4】予約の帯にカーソルを合わせたときの詳細表示を、ブラウザ標準
// のtitle属性（3秒程度）から、自前の吹き出し（1秒）に変更した（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/17追加】(1) PCのブラウザでは機材名欄をマウスでドラッグしても文字列選択が
// 先に発生してしまい横スライドが効かない不具合を修正（文字選択を禁止し、
// pointerdownでpreventDefault）。(2) ガントチャートの月移動を「前」「次」ボタン
// （30日ずつしか進めない）から、期間表示をタップして直接月を選べる方式に変更した
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/17追加 その2】タップしないと選べる月が見えないピッカー方式をやめ、
// 月のボタンを最初から横一列に並べて表示するようにした。入りきらない月は
// この行自体を指でスライドして見られる（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/18追加】ガントチャート本体を「次」「今月を選ぶ」ボタンを押さずに
// 指でスライドし続けるだけで、もっと先・もっと前の日付まで途中で止まらずに
// 見られるようにした（端に近づくたびに実際の表示日数を延長し、見た目の位置が
// ずれないようスクロール位置も補正する。上限は約3年）（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/18追加 その2】(1) スマホ・タブレットを横向きにしたときに左右の余白が
// 大きすぎた点を修正（横向きのときだけ画面の横幅をもっと使うようにした）。
// (2) 横向きのときはガントチャートの縦の表示範囲がとても狭くなっていた点を修正
// （ヘッダーやボタン類の余白を詰め、期間表示と月ボタンの行を1行にまとめ、
// 表本体の高さを実際の画面サイズから計算し直すようにした）。
// (3) あわせて、初回表示のたびに意図せず過去方向へ余分に延長されてしまっていた
// 不具合（(1)(2)の確認中に発覚）も修正した（index.html更新）。あわせて
// キャッシュ名も上げる。
// 【2026/9/18追加 その3】横向き＋ガントチャート表示のときは、これまでロゴ付き
// ヘッダー分だけ表の行が少なく見えていた（下に1回スクロールしてヘッダーが
// 隠れると、その分多く見えるようになっていた）。毎回手でスクロールしなくても
// 最初からその「隠れた後」の表示になるよう、この組み合わせのときだけヘッダーを
// 表示しないようにした（他のタブに切り替えれば今まで通り表示される）
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/18追加 その4】横向きにしたとき、実機で「画面の一番下に背景の写真が
// 少し見えてしまう（表本体の高さが実際より低く見積もられている）」「画面の
// 横幅を最後まで使い切れていない」という報告があったため再修正。(1) 表本体の
// 高さの計算に、ブラウザのアドレスバーの出入りをより正確に捉えられる
// window.visualViewportを使うようにし、タブ切り替え・画面回転のあとも
// 時間差で何度か測り直すようにした（アドレスバーの動きが収まるまで待つため）。
// あわせて、ホームインジケーターの分はenv(safe-area-inset-bottom)としてCSS側で
// 確保するようにした。(2) 横幅の上限（1100px）を撤廃し、画面幅いっぱいまで
// 使うようにした。あわせて、ノッチが左右に来る端末向けに、その分の
// safe-area-inset-left/rightもpaddingとして確保した（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/19追加】前回(v26)の横向き向け修正(幅の上限撤廃・visualViewport/
// safe-area-inset-bottomを使った高さ計算)が、実機ではかえって左右の余白・
// 画面下部の余白を増やす結果になってしまったとの報告があったため、その2点は
// 前々回(v25)の状態に戻した(幅は再び1100pxを上限に、高さはwindow.innerHeight
// だけを使うシンプルな計算に戻し、下に残す余白も10px→4pxに減らした)。
// あわせて、ガントチャートの日付ヘッダー行の高さを機材の行の半分にし、
// その分だけ機材の行がもう1行分見えるようにした(index.html更新)。
// あわせてキャッシュ名も上げる。
const CACHE_NAME = 'kizai-mobile-v27';
const APP_SHELL = [
  './',
  './index.html',
  './manifest.json',
  './icon-192.png',
  './icon-512.png'
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => cache.addAll(APP_SHELL))
  );
  self.skipWaiting();
});

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((names) => {
      return Promise.all(
        names.filter((name) => name !== CACHE_NAME).map((name) => caches.delete(name))
      );
    })
  );
  self.clients.claim();
});

self.addEventListener('fetch', (event) => {
  const url = event.request.url;

  // Apps ScriptのWeb App（データの取得・保存）は常にネットワークを優先する
  if (url.indexOf('script.google.com') !== -1 || url.indexOf('script.googleusercontent.com') !== -1) {
    event.respondWith(
      fetch(event.request).catch(() => new Response(
        JSON.stringify({ ok: false, error: 'オフラインのため通信できません' }),
        { headers: { 'Content-Type': 'application/json' } }
      ))
    );
    return;
  }

  // ページ本体（index.html）は「まずネットワークから最新版を取りに行き、取れなければキャッシュ」にする
  // （こうしないと、コード更新後も古い画面がキャッシュから表示され続けてしまうため）
  // 【2026/8/28修正】fetch()に何も指定しないと、ブラウザ自身が持っているHTTPキャッシュ
  // （Service WorkerのCache APIとは別物）がまだ「新しい」と判断されている間は、
  // ネットワークに問い合わせすら行かずそのキャッシュがそのまま返ってきてしまう
  // ことが分かった。GitHub Pagesは配信するファイルに数分程度のキャッシュ有効期限を
  // 付けているため、更新後しばらくは（ブラウザを開き直しても）以前の内容が
  // 表示され続けてしまっていた。{cache: 'no-store'}を指定し、常にネットワークへ
  // 直接問い合わせて本当に最新の内容を取りに行くようにした。
  if (event.request.mode === 'navigate' || url.indexOf('index.html') !== -1) {
    event.respondWith(
      fetch(event.request, { cache: 'no-store' })
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => caches.match(event.request))
    );
    return;
  }

  // アイコンやmanifestなど、めったに変わらないファイルは「キャッシュがあればまずそれを返しつつ、裏で最新版に更新」する
  event.respondWith(
    caches.match(event.request).then((cached) => {
      const fetchPromise = fetch(event.request)
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => cached);
      return cached || fetchPromise;
    })
  );
});
2026 モバイル用のサービスワーカー
// アプリの外枠（HTML/CSS/JS/アイコン）だけをキャッシュしておき、// 機材シート2026 モバイル用のサービスワーカー
// アプリの外枠（HTML/CSS/JS/アイコン）だけをキャッシュしておき、
// 電波が弱い場所でも起動できるようにする。// 機材シート2026 モバイル用のサービスワーカー
// アプリの外枠（HTML/CSS/JS/アイコン）だけをキャッシュしておき、
// 電波が弱い場所でも起動できるようにする。
// データそのもの（予約一覧など）は毎回ネットワークから取得する
// （キャッシュを見せてしまうと古いデータに気づけなくなるため）。

// 【2026/8/29修正】アイコン画像（icon-192.png/icon-512.png）を透明背景から白背景に
// 差し替えた（iOSのホーム画面アイコンは透明部分を黒く塗りつぶして表示してしまうため、
// 「渡したアイコンと違って見える」原因になっていた）。古いキャッシュに透明版の
// アイコンが残っている端末でも、確実に新しい画像を取りに行けるようキャッシュ名を
// 更新した。
// 【2026/9/3追加】ホーム画面に追加したスマホ（特にiPhone）で、機材のカテゴリー分け
// 機能を追加したあとも古い画面のまま変わらないという報告があった。index.html自体は
// 毎回ネットワークから取りに行く設計（下のfetchハンドラ参照）なので通常は問題ない
// はずだが、機種によってはservice worker（このファイル自体）の更新チェックが
// すぐには走らず、古いservice workerが動き続けてしまうことがある。合言葉の
// 再ログインではこの層のキャッシュは消えない（ログイン情報とservice workerの
// キャッシュは別物のため）。キャッシュ名を上げておくことで、次に新しい
// service workerへの切り替えが行われたタイミングで、確実に前のキャッシュ
// （kizai-mobile-v7以前）が破棄されるようにした。
// 【2026/9/12追加】アプリ内「ガントチャート」タブを追加（index.html更新）。
// 念のため、こちらもキャッシュ名を上げておく。
// 【2026/9/13追加】ガントチャートタブが表示されない不具合の調査・修正のため
// index.htmlを更新。あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その2】ガントチャートの見た目を修正（機材名の欄を左右スクロール
// 対応にし、上部の凡例を機材カテゴリーのショートカットボタンに差し替え）。
// index.htmlを更新したため、あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その3】機材名欄の横スクロールを全行連動にした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加】機材名欄の連動スクロールが実機で効かない不具合を修正。
// ブラウザ標準の横スクロールに頼らず、指のドラッグを直接読み取る方式に作り替えた
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その2】機材名欄のスワイプが重いという指摘に対応。ドラッグ中に
// 毎回文字の幅を測り直していたのをやめ、描画時に1回だけ測ってキャッシュし、画面への
// 反映もrequestAnimationFrameでまとめるようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その3】機材名が短い行だけ途中で止まって見える動きは不要とのことで、
// 全行が常に同じ量だけ動くように変更（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/15追加】指が機材名欄からガントチャート本体側にはみ出すと途中でスライドが
// 止まってしまう不具合を修正。タッチ操作は生のTouchイベントで直接扱い、指の位置に
// 関わらず同じ指である限りスライドを継続するようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/16追加】機材名欄で指を上下に動かしたときは、ガントチャート全体を上下に
// 見られるよう、標準の縦スクロールに任せるようにした（横方向のスライドとどちらの
// 操作か、最初の指の動きで判定する）（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その2】カレンダーの日付が見づらいという指摘に対応し、数字を
// はっきり黒くした。ガントチャートの日付も、1つ1つ枠で囲んで見やすくした
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その3】日付の枠をヘッダーだけでなく、ガントチャート本体（予約の帯が
// 並ぶ部分）にも入れ、縦の区切り線でどの日付か分かりやすくした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その4】予約の帯にカーソルを合わせたときの詳細表示を、ブラウザ標準
// のtitle属性（3秒程度）から、自前の吹き出し（1秒）に変更した（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/17追加】(1) PCのブラウザでは機材名欄をマウスでドラッグしても文字列選択が
// 先に発生してしまい横スライドが効かない不具合を修正（文字選択を禁止し、
// pointerdownでpreventDefault）。(2) ガントチャートの月移動を「前」「次」ボタン
// （30日ずつしか進めない）から、期間表示をタップして直接月を選べる方式に変更した
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/17追加 その2】タップしないと選べる月が見えないピッカー方式をやめ、
// 月のボタンを最初から横一列に並べて表示するようにした。入りきらない月は
// この行自体を指でスライドして見られる（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/18追加】ガントチャート本体を「次」「今月を選ぶ」ボタンを押さずに
// 指でスライドし続けるだけで、もっと先・もっと前の日付まで途中で止まらずに
// 見られるようにした（端に近づくたびに実際の表示日数を延長し、見た目の位置が
// ずれないようスクロール位置も補正する。上限は約3年）（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/18追加 その2】(1) スマホ・タブレットを横向きにしたときに左右の余白が
// 大きすぎた点を修正（横向きのときだけ画面の横幅をもっと使うようにした）。
// (2) 横向きのときはガントチャートの縦の表示範囲がとても狭くなっていた点を修正
// （ヘッダーやボタン類の余白を詰め、期間表示と月ボタンの行を1行にまとめ、
// 表本体の高さを実際の画面サイズから計算し直すようにした）。
// (3) あわせて、初回表示のたびに意図せず過去方向へ余分に延長されてしまっていた
// 不具合（(1)(2)の確認中に発覚）も修正した（index.html更新）。あわせて
// キャッシュ名も上げる。
// 【2026/9/18追加 その3】横向き＋ガントチャート表示のときは、これまでロゴ付き
// ヘッダー分だけ表の行が少なく見えていた（下に1回スクロールしてヘッダーが
// 隠れると、その分多く見えるようになっていた）。毎回手でスクロールしなくても
// 最初からその「隠れた後」の表示になるよう、この組み合わせのときだけヘッダーを
// 表示しないようにした（他のタブに切り替えれば今まで通り表示される）
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/18追加 その4】横向きにしたとき、実機で「画面の一番下に背景の写真が
// 少し見えてしまう（表本体の高さが実際より低く見積もられている）」「画面の
// 横幅を最後まで使い切れていない」という報告があったため再修正。(1) 表本体の
// 高さの計算に、ブラウザのアドレスバーの出入りをより正確に捉えられる
// window.visualViewportを使うようにし、タブ切り替え・画面回転のあとも
// 時間差で何度か測り直すようにした（アドレスバーの動きが収まるまで待つため）。
// あわせて、ホームインジケーターの分はenv(safe-area-inset-bottom)としてCSS側で
// 確保するようにした。(2) 横幅の上限（1100px）を撤廃し、画面幅いっぱいまで
// 使うようにした。あわせて、ノッチが左右に来る端末向けに、その分の
// safe-area-inset-left/rightもpaddingとして確保した（index.html更新）。
// あわせてキャッシュ名も上げる。
const CACHE_NAME = 'kizai-mobile-v26';
const APP_SHELL = [
  './',
  './index.html',
  './manifest.json',
  './icon-192.png',
  './icon-512.png'
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => cache.addAll(APP_SHELL))
  );
  self.skipWaiting();
});

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((names) => {
      return Promise.all(
        names.filter((name) => name !== CACHE_NAME).map((name) => caches.delete(name))
      );
    })
  );
  self.clients.claim();
});

self.addEventListener('fetch', (event) => {
  const url = event.request.url;

  // Apps ScriptのWeb App（データの取得・保存）は常にネットワークを優先する
  if (url.indexOf('script.google.com') !== -1 || url.indexOf('script.googleusercontent.com') !== -1) {
    event.respondWith(
      fetch(event.request).catch(() => new Response(
        JSON.stringify({ ok: false, error: 'オフラインのため通信できません' }),
        { headers: { 'Content-Type': 'application/json' } }
      ))
    );
    return;
  }

  // ページ本体（index.html）は「まずネットワークから最新版を取りに行き、取れなければキャッシュ」にする
  // （こうしないと、コード更新後も古い画面がキャッシュから表示され続けてしまうため）
  // 【2026/8/28修正】fetch()に何も指定しないと、ブラウザ自身が持っているHTTPキャッシュ
  // （Service WorkerのCache APIとは別物）がまだ「新しい」と判断されている間は、
  // ネットワークに問い合わせすら行かずそのキャッシュがそのまま返ってきてしまう
  // ことが分かった。GitHub Pagesは配信するファイルに数分程度のキャッシュ有効期限を
  // 付けているため、更新後しばらくは（ブラウザを開き直しても）以前の内容が
  // 表示され続けてしまっていた。{cache: 'no-store'}を指定し、常にネットワークへ
  // 直接問い合わせて本当に最新の内容を取りに行くようにした。
  if (event.request.mode === 'navigate' || url.indexOf('index.html') !== -1) {
    event.respondWith(
      fetch(event.request, { cache: 'no-store' })
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => caches.match(event.request))
    );
    return;
  }

  // アイコンやmanifestなど、めったに変わらないファイルは「キャッシュがあればまずそれを返しつつ、裏で最新版に更新」する
  event.respondWith(
    caches.match(event.request).then((cached) => {
      const fetchPromise = fetch(event.request)
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => cached);
      return cached || fetchPromise;
    })
  );
});

// データそのもの（予約一覧など）は毎回ネットワークから取得する
// （キャッシュを見せてしまうと古いデータに気づけなくなるため）。

// 【2026/8/29修正】アイコン画像（icon-192.png/icon-512.png）を透明背景から白背景に
// 差し替えた（iOSのホーム画面アイコンは透明部分を黒く塗りつぶして表示してしまうため、
// 「渡したアイコンと違って見える」原因になっていた）。古いキャッシュに透明版の
// アイコンが残っている端末でも、確実に新しい画像を取りに行けるようキャッシュ名を
// 更新した。
// 【2026/9/3追加】ホーム画面に追加したスマホ（特にiPhone）で、機材のカテゴリー分け
// 機能を追加したあとも古い画面のまま変わらないという報告があった。index.html自体は
// 毎回ネットワークから取りに行く設計（下のfetchハンドラ参照）なので通常は問題ない
// はずだが、機種によってはservice worker（このファイル自体）の更新チェックが
// すぐには走らず、古いservice workerが動き続けてしまうことがある。合言葉の
// 再ログインではこの層のキャッシュは消えない（ログイン情報とservice workerの
// キャッシュは別物のため）。キャッシュ名を上げておくことで、次に新しい
// service workerへの切り替えが行われたタイミングで、確実に前のキャッシュ
// （kizai-mobile-v7以前）が破棄されるようにした。
// 【2026/9/12追加】アプリ内「ガントチャート」タブを追加（index.html更新）。
// 念のため、こちらもキャッシュ名を上げておく。
// 【2026/9/13追加】ガントチャートタブが表示されない不具合の調査・修正のため
// index.htmlを更新。あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その2】ガントチャートの見た目を修正（機材名の欄を左右スクロール
// 対応にし、上部の凡例を機材カテゴリーのショートカットボタンに差し替え）。
// index.htmlを更新したため、あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その3】機材名欄の横スクロールを全行連動にした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加】機材名欄の連動スクロールが実機で効かない不具合を修正。
// ブラウザ標準の横スクロールに頼らず、指のドラッグを直接読み取る方式に作り替えた
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その2】機材名欄のスワイプが重いという指摘に対応。ドラッグ中に
// 毎回文字の幅を測り直していたのをやめ、描画時に1回だけ測ってキャッシュし、画面への
// 反映もrequestAnimationFrameでまとめるようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その3】機材名が短い行だけ途中で止まって見える動きは不要とのことで、
// 全行が常に同じ量だけ動くように変更（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/15追加】指が機材名欄からガントチャート本体側にはみ出すと途中でスライドが
// 止まってしまう不具合を修正。タッチ操作は生のTouchイベントで直接扱い、指の位置に
// 関わらず同じ指である限りスライドを継続するようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/16追加】機材名欄で指を上下に動かしたときは、ガントチャート全体を上下に
// 見られるよう、標準の縦スクロールに任せるようにした（横方向のスライドとどちらの
// 操作か、最初の指の動きで判定する）（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その2】カレンダーの日付が見づらいという指摘に対応し、数字を
// はっきり黒くした。ガントチャートの日付も、1つ1つ枠で囲んで見やすくした
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その3】日付の枠をヘッダーだけでなく、ガントチャート本体（予約の帯が
// 並ぶ部分）にも入れ、縦の区切り線でどの日付か分かりやすくした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その4】予約の帯にカーソルを合わせたときの詳細表示を、ブラウザ標準
// のtitle属性（3秒程度）から、自前の吹き出し（1秒）に変更した（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/17追加】(1) PCのブラウザでは機材名欄をマウスでドラッグしても文字列選択が
// 先に発生してしまい横スライドが効かない不具合を修正（文字選択を禁止し、
// pointerdownでpreventDefault）。(2) ガントチャートの月移動を「前」「次」ボタン
// （30日ずつしか進めない）から、期間表示をタップして直接月を選べる方式に変更した
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/17追加 その2】タップしないと選べる月が見えないピッカー方式をやめ、
// 月のボタンを最初から横一列に並べて表示するようにした。入りきらない月は
// この行自体を指でスライドして見られる（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/18追加】ガントチャート本体を「次」「今月を選ぶ」ボタンを押さずに
// 指でスライドし続けるだけで、もっと先・もっと前の日付まで途中で止まらずに
// 見られるようにした（端に近づくたびに実際の表示日数を延長し、見た目の位置が
// ずれないようスクロール位置も補正する。上限は約3年）（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/18追加 その2】(1) スマホ・タブレットを横向きにしたときに左右の余白が
// 大きすぎた点を修正（横向きのときだけ画面の横幅をもっと使うようにした）。
// (2) 横向きのときはガントチャートの縦の表示範囲がとても狭くなっていた点を修正
// （ヘッダーやボタン類の余白を詰め、期間表示と月ボタンの行を1行にまとめ、
// 表本体の高さを実際の画面サイズから計算し直すようにした）。
// (3) あわせて、初回表示のたびに意図せず過去方向へ余分に延長されてしまっていた
// 不具合（(1)(2)の確認中に発覚）も修正した（index.html更新）。あわせて
// キャッシュ名も上げる。
// 【2026/9/18追加 その3】横向き＋ガントチャート表示のときは、これまでロゴ付き
// ヘッダー分だけ表の行が少なく見えていた（下に1回スクロールしてヘッダーが
// 隠れると、その分多く見えるようになっていた）。毎回手でスクロールしなくても
// 最初からその「隠れた後」の表示になるよう、この組み合わせのときだけヘッダーを
// 表示しないようにした（他のタブに切り替えれば今まで通り表示される）
// （index.html更新）。あわせてキャッシュ名も上げる。
const CACHE_NAME = 'kizai-mobile-v25';
const APP_SHELL = [
  './',
  './index.html',
  './manifest.json',
  './icon-192.png',
  './icon-512.png'
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => cache.addAll(APP_SHELL))
  );
  self.skipWaiting();
});

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((names) => {
      return Promise.all(
        names.filter((name) => name !== CACHE_NAME).map((name) => caches.delete(name))
      );
    })
  );
  self.clients.claim();
});

self.addEventListener('fetch', (event) => {
  const url = event.request.url;

  // Apps ScriptのWeb App（データの取得・保存）は常にネットワークを優先する
  if (url.indexOf('script.google.com') !== -1 || url.indexOf('script.googleusercontent.com') !== -1) {
    event.respondWith(
      fetch(event.request).catch(() => new Response(
        JSON.stringify({ ok: false, error: 'オフラインのため通信できません' }),
        { headers: { 'Content-Type': 'application/json' } }
      ))
    );
    return;
  }

  // ページ本体（index.html）は「まずネットワークから最新版を取りに行き、取れなければキャッシュ」にする
  // （こうしないと、コード更新後も古い画面がキャッシュから表示され続けてしまうため）
  // 【2026/8/28修正】fetch()に何も指定しないと、ブラウザ自身が持っているHTTPキャッシュ
  // （Service WorkerのCache APIとは別物）がまだ「新しい」と判断されている間は、
  // ネットワークに問い合わせすら行かずそのキャッシュがそのまま返ってきてしまう
  // ことが分かった。GitHub Pagesは配信するファイルに数分程度のキャッシュ有効期限を
  // 付けているため、更新後しばらくは（ブラウザを開き直しても）以前の内容が
  // 表示され続けてしまっていた。{cache: 'no-store'}を指定し、常にネットワークへ
  // 直接問い合わせて本当に最新の内容を取りに行くようにした。
  if (event.request.mode === 'navigate' || url.indexOf('index.html') !== -1) {
    event.respondWith(
      fetch(event.request, { cache: 'no-store' })
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => caches.match(event.request))
    );
    return;
  }

  // アイコンやmanifestなど、めったに変わらないファイルは「キャッシュがあればまずそれを返しつつ、裏で最新版に更新」する
  event.respondWith(
    caches.match(event.request).then((cached) => {
      const fetchPromise = fetch(event.request)
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => cached);
      return cached || fetchPromise;
    })
  );
});

// 電波が弱い場所でも起動できるようにする。
// データそのもの（予約一覧など）は毎回ネットワークから取得する
// （キャッシュを見せてしまうと古いデータに気づけなくなるため）。

// 【2026/8/29修正】アイコン画像（icon-192.png/icon-512.png）を透明背景から白背景に
// 差し替えた（iOSのホーム画面アイコンは透明部分を黒く塗りつぶして表示してしまうため、
// 「渡したアイコンと違って見える」原因になっていた）。古いキャッシュに透明版の
// アイコンが残っている端末でも、確実に新しい画像を取りに行けるようキャッシュ名を
// 更新した。
// 【2026/9/3追加】ホーム画面に追加したスマホ（特にiPhone）で、機材のカテゴリー分け
// 機能を追加したあとも古い画面のまま変わらないという報告があった。index.html自体は
// 毎回ネットワークから取りに行く設計（下のfetchハンドラ参照）なので通常は問題ない
// はずだが、機種によってはservice worker（このファイル自体）の更新チェックが
// すぐには走らず、古いservice workerが動き続けてしまうことがある。合言葉の
// 再ログインではこの層のキャッシュは消えない（ログイン情報とservice workerの
// キャッシュは別物のため）。キャッシュ名を上げておくことで、次に新しい
// service workerへの切り替えが行われたタイミングで、確実に前のキャッシュ
// （kizai-mobile-v7以前）が破棄されるようにした。
// 【2026/9/12追加】アプリ内「ガントチャート」タブを追加（index.html更新）。
// 念のため、こちらもキャッシュ名を上げておく。
// 【2026/9/13追加】ガントチャートタブが表示されない不具合の調査・修正のため
// index.htmlを更新。あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その2】ガントチャートの見た目を修正（機材名の欄を左右スクロール
// 対応にし、上部の凡例を機材カテゴリーのショートカットボタンに差し替え）。
// index.htmlを更新したため、あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その3】機材名欄の横スクロールを全行連動にした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加】機材名欄の連動スクロールが実機で効かない不具合を修正。
// ブラウザ標準の横スクロールに頼らず、指のドラッグを直接読み取る方式に作り替えた
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その2】機材名欄のスワイプが重いという指摘に対応。ドラッグ中に
// 毎回文字の幅を測り直していたのをやめ、描画時に1回だけ測ってキャッシュし、画面への
// 反映もrequestAnimationFrameでまとめるようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その3】機材名が短い行だけ途中で止まって見える動きは不要とのことで、
// 全行が常に同じ量だけ動くように変更（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/15追加】指が機材名欄からガントチャート本体側にはみ出すと途中でスライドが
// 止まってしまう不具合を修正。タッチ操作は生のTouchイベントで直接扱い、指の位置に
// 関わらず同じ指である限りスライドを継続するようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/16追加】機材名欄で指を上下に動かしたときは、ガントチャート全体を上下に
// 見られるよう、標準の縦スクロールに任せるようにした（横方向のスライドとどちらの
// 操作か、最初の指の動きで判定する）（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その2】カレンダーの日付が見づらいという指摘に対応し、数字を
// はっきり黒くした。ガントチャートの日付も、1つ1つ枠で囲んで見やすくした
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その3】日付の枠をヘッダーだけでなく、ガントチャート本体（予約の帯が
// 並ぶ部分）にも入れ、縦の区切り線でどの日付か分かりやすくした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その4】予約の帯にカーソルを合わせたときの詳細表示を、ブラウザ標準
// のtitle属性（3秒程度）から、自前の吹き出し（1秒）に変更した（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/17追加】(1) PCのブラウザでは機材名欄をマウスでドラッグしても文字列選択が
// 先に発生してしまい横スライドが効かない不具合を修正（文字選択を禁止し、
// pointerdownでpreventDefault）。(2) ガントチャートの月移動を「前」「次」ボタン
// （30日ずつしか進めない）から、期間表示をタップして直接月を選べる方式に変更した
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/17追加 その2】タップしないと選べる月が見えないピッカー方式をやめ、
// 月のボタンを最初から横一列に並べて表示するようにした。入りきらない月は
// この行自体を指でスライドして見られる（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/18追加】ガントチャート本体を「次」「今月を選ぶ」ボタンを押さずに
// 指でスライドし続けるだけで、もっと先・もっと前の日付まで途中で止まらずに
// 見られるようにした（端に近づくたびに実際の表示日数を延長し、見た目の位置が
// ずれないようスクロール位置も補正する。上限は約3年）（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/18追加 その2】(1) スマホ・タブレットを横向きにしたときに左右の余白が
// 大きすぎた点を修正（横向きのときだけ画面の横幅をもっと使うようにした）。
// (2) 横向きのときはガントチャートの縦の表示範囲がとても狭くなっていた点を修正
// （ヘッダーやボタン類の余白を詰め、期間表示と月ボタンの行を1行にまとめ、
// 表本体の高さを実際の画面サイズから計算し直すようにした）。
// (3) あわせて、初回表示のたびに意図せず過去方向へ余分に延長されてしまっていた
// 不具合（(1)(2)の確認中に発覚）も修正した（index.html更新）。あわせて
// キャッシュ名も上げる。
const CACHE_NAME = 'kizai-mobile-v24';
const APP_SHELL = [
  './',
  './index.html',
  './manifest.json',
  './icon-192.png',
  './icon-512.png'
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => cache.addAll(APP_SHELL))
  );
  self.skipWaiting();
});

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((names) => {
      return Promise.all(
        names.filter((name) => name !== CACHE_NAME).map((name) => caches.delete(name))
      );
    })
  );
  self.clients.claim();
});

self.addEventListener('fetch', (event) => {
  const url = event.request.url;

  // Apps ScriptのWeb App（データの取得・保存）は常にネットワークを優先する
  if (url.indexOf('script.google.com') !== -1 || url.indexOf('script.googleusercontent.com') !== -1) {
    event.respondWith(
      fetch(event.request).catch(() => new Response(
        JSON.stringify({ ok: false, error: 'オフラインのため通信できません' }),
        { headers: { 'Content-Type': 'application/json' } }
      ))
    );
    return;
  }

  // ページ本体（index.html）は「まずネットワークから最新版を取りに行き、取れなければキャッシュ」にする
  // （こうしないと、コード更新後も古い画面がキャッシュから表示され続けてしまうため）
  // 【2026/8/28修正】fetch()に何も指定しないと、ブラウザ自身が持っているHTTPキャッシュ
  // （Service WorkerのCache APIとは別物）がまだ「新しい」と判断されている間は、
  // ネットワークに問い合わせすら行かずそのキャッシュがそのまま返ってきてしまう
  // ことが分かった。GitHub Pagesは配信するファイルに数分程度のキャッシュ有効期限を
  // 付けているため、更新後しばらくは（ブラウザを開き直しても）以前の内容が
  // 表示され続けてしまっていた。{cache: 'no-store'}を指定し、常にネットワークへ
  // 直接問い合わせて本当に最新の内容を取りに行くようにした。
  if (event.request.mode === 'navigate' || url.indexOf('index.html') !== -1) {
    event.respondWith(
      fetch(event.request, { cache: 'no-store' })
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => caches.match(event.request))
    );
    return;
  }

  // アイコンやmanifestなど、めったに変わらないファイルは「キャッシュがあればまずそれを返しつつ、裏で最新版に更新」する
  event.respondWith(
    caches.match(event.request).then((cached) => {
      const fetchPromise = fetch(event.request)
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => cached);
      return cached || fetchPromise;
    })
  );
});
るた// 機材シート2026 モバイル用のサービスワーカー
// アプリの外枠（HTML/CSS/JS/アイコン）だけをキャッシュしておき、
// 電波が弱い場所でも起動できるようにする。
// データそのもの（予約一覧など）は毎回ネットワークから取得する
// （キャッシュを見せてしまうと古いデータに気づけなくな// 機材シート2026 モバイル用のサービスワーカー
// アプリの外枠（HTML/CSS/JS/アイコン）だけをキャッシュしておき、
// 電波が弱い場所でも起動できるようにする。
// データそのもの（予約一覧など）は毎回ネットワークから取得する
// （キャッシュを見せてしまうと古いデータに気づけなくなるため）。

// 【2026/8/29修正】アイコン画像（icon-192.png/icon-512.png）を透明背景から白背景に
// 差し替えた（iOSのホーム画面アイコンは透明部分を黒く塗りつぶして表示してしまうため、
// 「渡したアイコンと違って見える」原因になっていた）。古いキャッシュに透明版の
// アイコンが残っている端末でも、確実に新しい画像を取りに行けるようキャッシュ名を
// 更新した。
// 【2026/9/3追加】ホーム画面に追加したスマホ（特にiPhone）で、機材のカテゴリー分け
// 機能を追加したあとも古い画面のまま変わらないという報告があった。index.html自体は
// 毎回ネットワークから取りに行く設計（下のfetchハンドラ参照）なので通常は問題ない
// はずだが、機種によってはservice worker（このファイル自体）の更新チェックが
// すぐには走らず、古いservice workerが動き続けてしまうことがある。合言葉の
// 再ログインではこの層のキャッシュは消えない（ログイン情報とservice workerの
// キャッシュは別物のため）。キャッシュ名を上げておくことで、次に新しい
// service workerへの切り替えが行われたタイミングで、確実に前のキャッシュ
// （kizai-mobile-v7以前）が破棄されるようにした。
// 【2026/9/12追加】アプリ内「ガントチャート」タブを追加（index.html更新）。
// 念のため、こちらもキャッシュ名を上げておく。
// 【2026/9/13追加】ガントチャートタブが表示されない不具合の調査・修正のため
// index.htmlを更新。あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その2】ガントチャートの見た目を修正（機材名の欄を左右スクロール
// 対応にし、上部の凡例を機材カテゴリーのショートカットボタンに差し替え）。
// index.htmlを更新したため、あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その3】機材名欄の横スクロールを全行連動にした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加】機材名欄の連動スクロールが実機で効かない不具合を修正。
// ブラウザ標準の横スクロールに頼らず、指のドラッグを直接読み取る方式に作り替えた
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その2】機材名欄のスワイプが重いという指摘に対応。ドラッグ中に
// 毎回文字の幅を測り直していたのをやめ、描画時に1回だけ測ってキャッシュし、画面への
// 反映もrequestAnimationFrameでまとめるようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その3】機材名が短い行だけ途中で止まって見える動きは不要とのことで、
// 全行が常に同じ量だけ動くように変更（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/15追加】指が機材名欄からガントチャート本体側にはみ出すと途中でスライドが
// 止まってしまう不具合を修正。タッチ操作は生のTouchイベントで直接扱い、指の位置に
// 関わらず同じ指である限りスライドを継続するようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/16追加】機材名欄で指を上下に動かしたときは、ガントチャート全体を上下に
// 見られるよう、標準の縦スクロールに任せるようにした（横方向のスライドとどちらの
// 操作か、最初の指の動きで判定する）（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その2】カレンダーの日付が見づらいという指摘に対応し、数字を
// はっきり黒くした。ガントチャートの日付も、1つ1つ枠で囲んで見やすくした
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その3】日付の枠をヘッダーだけでなく、ガントチャート本体（予約の帯が
// 並ぶ部分）にも入れ、縦の区切り線でどの日付か分かりやすくした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その4】予約の帯にカーソルを合わせたときの詳細表示を、ブラウザ標準
// のtitle属性（3秒程度）から、自前の吹き出し（1秒）に変更した（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/17追加】(1) PCのブラウザでは機材名欄をマウスでドラッグしても文字列選択が
// 先に発生してしまい横スライドが効かない不具合を修正（文字選択を禁止し、
// pointerdownでpreventDefault）。(2) ガントチャートの月移動を「前」「次」ボタン
// （30日ずつしか進めない）から、期間表示をタップして直接月を選べる方式に変更した
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/17追加 その2】タップしないと選べる月が見えないピッカー方式をやめ、
// 月のボタンを最初から横一列に並べて表示するようにした。入りきらない月は
// この行自体を指でスライドして見られる（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/18追加】ガントチャート本体を「次」「今月を選ぶ」ボタンを押さずに
// 指でスライドし続けるだけで、もっと先・もっと前の日付まで途中で止まらずに
// 見られるようにした（端に近づくたびに実際の表示日数を延長し、見た目の位置が
// ずれないようスクロール位置も補正する。上限は約3年）（index.html更新）。
// あわせてキャッシュ名も上げる。
const CACHE_NAME = 'kizai-mobile-v23';
const APP_SHELL = [
  './',
  './index.html',
  './manifest.json',
  './icon-192.png',
  './icon-512.png'
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => cache.addAll(APP_SHELL))
  );
  self.skipWaiting();
});

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((names) => {
      return Promise.all(
        names.filter((name) => name !== CACHE_NAME).map((name) => caches.delete(name))
      );
    })
  );
  self.clients.claim();
});

self.addEventListener('fetch', (event) => {
  const url = event.request.url;

  // Apps ScriptのWeb App（データの取得・保存）は常にネットワークを優先する
  if (url.indexOf('script.google.com') !== -1 || url.indexOf('script.googleusercontent.com') !== -1) {
    event.respondWith(
      fetch(event.request).catch(() => new Response(
        JSON.stringify({ ok: false, error: 'オフラインのため通信できません' }),
        { headers: { 'Content-Type': 'application/json' } }
      ))
    );
    return;
  }

  // ページ本体（index.html）は「まずネットワークから最新版を取りに行き、取れなければキャッシュ」にする
  // （こうしないと、コード更新後も古い画面がキャッシュから表示され続けてしまうため）
  // 【2026/8/28修正】fetch()に何も指定しないと、ブラウザ自身が持っているHTTPキャッシュ
  // （Service WorkerのCache APIとは別物）がまだ「新しい」と判断されている間は、
  // ネットワークに問い合わせすら行かずそのキャッシュがそのまま返ってきてしまう
  // ことが分かった。GitHub Pagesは配信するファイルに数分程度のキャッシュ有効期限を
  // 付けているため、更新後しばらくは（ブラウザを開き直しても）以前の内容が
  // 表示され続けてしまっていた。{cache: 'no-store'}を指定し、常にネットワークへ
  // 直接問い合わせて本当に最新の内容を取りに行くようにした。
  if (event.request.mode === 'navigate' || url.indexOf('index.html') !== -1) {
    event.respondWith(
      fetch(event.request, { cache: 'no-store' })
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => caches.match(event.request))
    );
    return;
  }

  // アイコンやmanifestなど、めったに変わらないファイルは「キャッシュがあればまずそれを返しつつ、裏で最新版に更新」する
  event.respondWith(
    caches.match(event.request).then((cached) => {
      const fetchPromise = fetch(event.request)
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => cached);
      return cached || fetchPromise;
    })
  );
});
るため）。

// 【2026/8/29修正】アイコン画像（icon-192.png/icon-512.png）を透明背景から白背景に
// 差し替えた（iOSのホーム画面アイコンは透明部分を黒く塗りつぶして表示してしまうため、
// 「渡したアイコンと違って見える」原因になっていた）。古いキャッシュに透明版の
// アイコンが残っている端末でも、確実に新しい画像を取りに行けるようキャッシュ名を
// 更新した。
// 【2026/9/3追加】ホーム画面に追加したスマホ（特にiPhone）で、機材のカテゴリー分け
// 機能を追加したあとも古い画面のまま変わらないという報告があった。index.html自体は
// 毎回ネットワークから取りに行く設計（下のfetchハンドラ参照）なので通常は問題ない
// はずだが、機種によってはservice worker（このファイル自体）の更新チェックが
// すぐには走らず、古いservice workerが動き続けてしまうことがある。合言葉の
// 再ログインではこの層のキャッシュは消えない（ログイン情報とservice workerの
// キャッシュは別物のため）。キャッシュ名を上げておくことで、次に新しい
// service workerへの切り替えが行われたタイミングで、確実に前のキャッシュ
// （kizai-mobile-v7以前）が破棄されるようにした。
// 【2026/9/12追加】アプリ内「ガントチャート」タブを追加（index.html更新）。
// 念のため、こちらもキャッシュ名を上げておく。
// 【2026/9/13追加】ガントチャートタブが表示されない不具合の調査・修正のため
// index.htmlを更新。あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その2】ガントチャートの見た目を修正（機材名の欄を左右スクロール
// 対応にし、上部の凡例を機材カテゴリーのショートカットボタンに差し替え）。
// index.htmlを更新したため、あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その3】機材名欄の横スクロールを全行連動にした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加】機材名欄の連動スクロールが実機で効かない不具合を修正。
// ブラウザ標準の横スクロールに頼らず、指のドラッグを直接読み取る方式に作り替えた
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その2】機材名欄のスワイプが重いという指摘に対応。ドラッグ中に
// 毎回文字の幅を測り直していたのをやめ、描画時に1回だけ測ってキャッシュし、画面への
// 反映もrequestAnimationFrameでまとめるようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その3】機材名が短い行だけ途中で止まって見える動きは不要とのことで、
// 全行が常に同じ量だけ動くように変更（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/15追加】指が機材名欄からガントチャート本体側にはみ出すと途中でスライドが
// 止まってしまう不具合を修正。タッチ操作は生のTouchイベントで直接扱い、指の位置に
// 関わらず同じ指である限りスライドを継続するようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/16追加】機材名欄で指を上下に動かしたときは、ガントチャート全体を上下に
// 見られるよう、標準の縦スクロールに任せるようにした（横方向のスライドとどちらの
// 操作か、最初の指の動きで判定する）（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その2】カレンダーの日付が見づらいという指摘に対応し、数字を
// はっきり黒くした。ガントチャートの日付も、1つ1つ枠で囲んで見やすくした
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その3】日付の枠をヘッダーだけでなく、ガントチャート本体（予約の帯が
// 並ぶ部分）にも入れ、縦の区切り線でどの日付か分かりやすくした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その4】予約の帯にカーソルを合わせたときの詳細表示を、ブラウザ標準
// のtitle属性（3秒程度）から、自前の吹き出し（1秒）に変更した（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/17追加】(1) PCのブラウザでは機材名欄をマウスでドラッグしても文字列選択が
// 先に発生してしまい横スライドが効かない不具合を修正（文字選択を禁止し、
// pointerdownでpreventDefault）。(2) ガントチャートの月移動を「前」「次」ボタン
// （30日ずつしか進めない）から、期間表示をタップして直接月を選べる方式に変更した
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/17追加 その2】タップしないと選べる月が見えないピッカー方式をやめ、
// 月のボタンを最初から横一列に並べて表示するようにした。入りきらない月は
// この行自体を指でスライドして見られる（index.html更新）。あわせてキャッシュ名も上げる。
const CACHE_NAME = 'kizai-mobile-v22';
const APP_SHELL = [
  './',
  './index.html',
  './manifest.json',
  './icon-192.png',
  './icon-512.png'
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => cache.addAll(APP_SHELL))
  );
  self.skipWaiting();
});

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((names) => {
      return Promise.all(
        names.filter((name) => name !== CACHE_NAME).map((name) => caches.delete(name))
      );
    })
  );
  self.clients.claim();
});

self.addEventListener('fetch', (event) => {
  const url = event.request.url;

  // Apps ScriptのWeb App（データの取得・保存）は常にネットワークを優先する
  if (url.indexOf('script.google.com') !== -1 || url.indexOf('script.googleusercontent.com') !== -1) {
    event.respondWith(
      fetch(event.request).catch(() => new Response(
        JSON.stringify({ ok: false, error: 'オフラインのため通信できません' }),
        { headers: { 'Content-Type': 'application/json' } }
      ))
    );
    return;
  }

  // ページ本体（index.html）は「まずネットワークから最新版を取りに行き、取れなければキャッシュ」にする
  // （こうしないと、コード更新後も古い画面がキャッシュから表示され続けてしまうため）
  // 【2026/8/28修正】fetch()に何も指定しないと、ブラウザ自身が持っているHTTPキャッシュ
  // （Service WorkerのCache APIとは別物）がまだ「新しい」と判断されている間は、
  // ネットワークに問い合わせすら行かずそのキャッシュがそのまま返ってきてしまう
  // ことが分かった。GitHub Pagesは配信するファイルに数分程度のキャッシュ有効期限を
  // 付けているため、更新後しばらくは（ブラウザを開き直しても）以前の内容が
  // 表示され続けてしまっていた。{cache: 'no-store'}を指定し、常にネットワークへ
  // 直接問い合わせて本当に最新の内容を取りに行くようにした。
  if (event.request.mode === 'navigate' || url.indexOf('index.html') !== -1) {
    event.respondWith(
      fetch(event.request, { cache: 'no-store' })
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => caches.match(event.request))
    );
    return;
  }

  // アイコンやmanifestなど、めったに変わらないファイルは「キャッシュがあればまずそれを返しつつ、裏で最新版に更新」する
  event.respondWith(
    caches.match(event.request).then((cached) => {
      const fetchPromise = fetch(event.request)
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => cached);
      return cached || fetchPromise;
    })
  );
});
// 機材シート2026 モバイル用のサービスワーカー
// アプリの外枠（HTML/CSS/JS/アイコン）だけをキャッシュしておき、
// 電波が弱い場所でも起動できるようにする。
// データそのもの（予約一覧など）は毎回ネットワークから取得する
// （キャッシュを見せてしまうと古いデータに気づけなくなるため）。

// 【2026/8/29修正】アイコン画像（icon-192.png/icon-512.png）を透明背景から白背景に
// 差し替えた（iOSのホーム画面アイコンは透明部分を黒く塗りつぶして表示してしまうため、
// 「渡したアイコンと違って見える」原因になっていた）。古いキャッシュに透明版の
// アイコンが残っている端末でも、確実に新しい画像を取りに行けるようキャッシュ名を
// 更新した。
// 【2026/9/3追加】ホーム画面に追加したスマホ（特にiPhone）で、機材のカテゴリー分け
// 機能を追加したあとも古い画面のまま変わらないという報告があった。index.html自体は
// 毎回ネットワークから取りに行く設計（下のfetchハンドラ参照）なので通常は問題ない
// はずだが、機種によってはservice worker（このファイル自体）の更新チェックが
// すぐには走らず、古いservice workerが動き続けてしまうことがある。合言葉の
// 再ログインではこの層のキャッシュは消えない（ログイン情報とservice workerの
// キャッシュは別物のため）。キャッシュ名を上げておくことで、次に新しい
// service workerへの切り替えが行われたタイミングで、確実に前のキャッシュ
// （kizai-mobile-v7以前）が破棄されるようにした。
// 【2026/9/12追加】アプリ内「ガントチャート」タブを追加（index.html更新）。
// 念のため、こちらもキャッシュ名を上げておく。
// 【2026/9/13追加】ガントチャートタブが表示されない不具合の調査・修正のため
// index.htmlを更新。あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その2】ガントチャートの見た目を修正（機材名の欄を左右スクロール
// 対応にし、上部の凡例を機材カテゴリーのショートカットボタンに差し替え）。
// index.htmlを更新したため、あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その3】機材名欄の横スクロールを全行連動にした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加】機材名欄の連動スクロールが実機で効かない不具合を修正。
// ブラウザ標準の横スクロールに頼らず、指のドラッグを直接読み取る方式に作り替えた
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その2】機材名欄のスワイプが重いという指摘に対応。ドラッグ中に
// 毎回文字の幅を測り直していたのをやめ、描画時に1回だけ測ってキャッシュし、画面への
// 反映もrequestAnimationFrameでまとめるようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その3】機材名が短い行だけ途中で止まって見える動きは不要とのことで、
// 全行が常に同じ量だけ動くように変更（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/15追加】指が機材名欄からガントチャート本体側にはみ出すと途中でスライドが
// 止まってしまう不具合を修正。タッチ操作は生のTouchイベントで直接扱い、指の位置に
// 関わらず同じ指である限りスライドを継続するようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/16追加】機材名欄で指を上下に動かしたときは、ガントチャート全体を上下に
// 見られるよう、標準の縦スクロールに任せるようにした（横方向のスライドとどちらの
// 操作か、最初の指の動きで判定する）（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その2】カレンダーの日付が見づらいという指摘に対応し、数字を
// はっきり黒くした。ガントチャートの日付も、1つ1つ枠で囲んで見やすくした
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その3】日付の枠をヘッダーだけでなく、ガントチャート本体（予約の帯が
// 並ぶ部分）にも入れ、縦の区切り線でどの日付か分かりやすくした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その4】予約の帯にカーソルを合わせたときの詳細表示を、ブラウザ標準
// のtitle属性（3秒程度）から、自前の吹き出し（1秒）に変更した（index.html更新）。
// あわせてキャッシュ名も上げる。
const CACHE_NAME = 'kizai-mobile-v20';
const APP_SHELL = [
  './',
  './index.html',
  './manifest.json',
  './icon-192.png',
  './icon-512.png'
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => cache.addAll(APP_SHELL))
  );
  self.skipWaiting();
});

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((names) => {
      return Promise.all(
        names.filter((name) => name !== CACHE_NAME).map((name) => caches.delete(name))
      );
    })
  );
  self.clients.claim();
});

self.addEventListener('fetch', (event) => {
  const url = event.request.url;

  // Apps ScriptのWeb App（データの取得・保存）は常にネットワークを優先する
  if (url.indexOf('script.google.com') !== -1 || url.indexOf('script.googleusercontent.com') !== -1) {
    event.respondWith(
      fetch(event.request).catch(() => new Response(
        JSON.stringify({ ok: false, error: 'オフラインのため通信できません' }),
        { headers: { 'Content-Type': 'application/json' } }
      ))
    );
    return;
  }

  // ページ本体（index.html）は「まずネットワークから最新版を取りに行き、取れなければキャッシュ」にする
  // （こうしないと、コード更新後も古い画面がキャッシュから表示され続けてしまうため）
  // 【2026/8/28修正】fetch()に何も指定しないと、ブラウザ自身が持っているHTTPキャッシュ
  // （Service WorkerのCache APIとは別物）がまだ「新しい」と判断されている間は、
  // ネットワークに問い合わせすら行かずそのキャッシュがそのまま返ってきてしまう
  // ことが分かった。GitHub Pagesは配信するファイルに数分程度のキャッシュ有効期限を
  // 付けているため、更新後しばらくは（ブラウザを開き直しても）以前の内容が
  // 表示され続けてしまっていた。{cache: 'no-store'}を指定し、常にネットワークへ
  // 直接問い合わせて本当に最新の内容を取りに行くようにした。
  if (event.request.mode === 'navigate' || url.indexOf('index.html') !== -1) {
    event.respondWith(
      fetch(event.request, { cache: 'no-store' })
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => caches.match(event.request))
    );
    return;
  }

  // アイコンやmanifestなど、めったに変わらないファイルは「キャッシュがあればまずそれを返しつつ、裏で最新版に更新」する
  event.respondWith(
    caches.match(event.request).then((cached) => {
      const fetchPromise = fetch(event.request)
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => cached);
      return cached || fetchPromise;
    })
  );
});
め）。

// 【2026/8/29修正】アイコン画像（icon-192.png/icon-512.png）を透明背景から白背景に
// 差し替えた（iOSのホーム画面アイコンは透明部分を黒く塗りつぶして表示してしまうため、
// 「渡したアイコンと違って見える」原因になっていた// 機材シート2026 モバイル用のサービスワーカー
// アプリの外枠（HTML/CSS/JS/アイコン）だけをキャッシュしておき、
// 電波が弱い場所でも起動できるようにする。
// データそのもの（予約一覧など）は毎回ネットワークから取得する
// （キャッシュを見せてしまうと古いデータに気づけなくなるため）。

// 【2026/8/29修正】アイコン画像（icon-192.png/icon-512.png）を透明背景から白背景に
// 差し替えた（iOSのホーム画面アイコンは透明部分を黒く塗りつぶして表示してしまうため、
// 「渡したアイコンと違って見える」原因になっていた）。古いキャッシュに透明版の
// アイコンが残っている端末でも、確実に新しい画像を取りに行けるようキャッシュ名を
// 更新した。
// 【2026/9/3追加】ホーム画面に追加したスマホ（特にiPhone）で、機材のカテゴリー分け
// 機能を追加したあとも古い画面のまま変わらないという報告があった。index.html自体は
// 毎回ネットワークから取りに行く設計（下のfetchハンドラ参照）なので通常は問題ない
// はずだが、機種によってはservice worker（このファイル自体）の更新チェックが
// すぐには走らず、古いservice workerが動き続けてしまうことがある。合言葉の
// 再ログインではこの層のキャッシュは消えない（ログイン情報とservice workerの
// キャッシュは別物のため）。キャッシュ名を上げておくことで、次に新しい
// service workerへの切り替えが行われたタイミングで、確実に前のキャッシュ
// （kizai-mobile-v7以前）が破棄されるようにした。
// 【2026/9/12追加】アプリ内「ガントチャート」タブを追加（index.html更新）。
// 念のため、こちらもキャッシュ名を上げておく。
// 【2026/9/13追加】ガントチャートタブが表示されない不具合の調査・修正のため
// index.htmlを更新。あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その2】ガントチャートの見た目を修正（機材名の欄を左右スクロール
// 対応にし、上部の凡例を機材カテゴリーのショートカットボタンに差し替え）。
// index.htmlを更新したため、あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その3】機材名欄の横スクロールを全行連動にした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加】機材名欄の連動スクロールが実機で効かない不具合を修正。
// ブラウザ標準の横スクロールに頼らず、指のドラッグを直接読み取る方式に作り替えた
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その2】機材名欄のスワイプが重いという指摘に対応。ドラッグ中に
// 毎回文字の幅を測り直していたのをやめ、描画時に1回だけ測ってキャッシュし、画面への
// 反映もrequestAnimationFrameでまとめるようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その3】機材名が短い行だけ途中で止まって見える動きは不要とのことで、
// 全行が常に同じ量だけ動くように変更（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/15追加】指が機材名欄からガントチャート本体側にはみ出すと途中でスライドが
// 止まってしまう不具合を修正。タッチ操作は生のTouchイベントで直接扱い、指の位置に
// 関わらず同じ指である限りスライドを継続するようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/16追加】機材名欄で指を上下に動かしたときは、ガントチャート全体を上下に
// 見られるよう、標準の縦スクロールに任せるようにした（横方向のスライドとどちらの
// 操作か、最初の指の動きで判定する）（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その2】カレンダーの日付が見づらいという指摘に対応し、数字を
// はっきり黒くした。ガントチャートの日付も、1つ1つ枠で囲んで見やすくした
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その3】日付の枠をヘッダーだけでなく、ガントチャート本体（予約の帯が
// 並ぶ部分）にも入れ、縦の区切り線でどの日付か分かりやすくした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その4】予約の帯にカーソルを合わせたときの詳細表示を、ブラウザ標準
// のtitle属性（3秒程度）から、自前の吹き出し（1秒）に変更した（index.html更新）。
// あわせてキャッシュ名も上げる。
const CACHE_NAME = 'kizai-mobile-v20';
const APP_SHELL = [
  './',
  './index.html',
  './manifest.json',
  './icon-192.png',
  './icon-512.png'
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => cache.addAll(APP_SHELL))
  );
  self.skipWaiting();
});

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((names) => {
      return Promise.all(
        names.filter((name) => name !== CACHE_NAME).map((name) => caches.delete(name))
      );
    })
  );
  self.clients.claim();
});

self.addEventListener('fetch', (event) => {
  const url = event.request.url;

  // Apps ScriptのWeb App（データの取得・保存）は常にネットワークを優先する
  if (url.indexOf('script.google.com') !== -1 || url.indexOf('script.googleusercontent.com') !== -1) {
    event.respondWith(
      fetch(event.request).catch(() => new Response(
        JSON.stringify({ ok: false, error: 'オフラインのため通信できません' }),
        { headers: { 'Content-Type': 'application/json' } }
      ))
    );
    return;
  }

  // ページ本体（index.html）は「まずネットワークから最新版を取りに行き、取れなければキャッシュ」にする
  // （こうしないと、コード更新後も古い画面がキャッシュから表示され続けてしまうため）
  // 【2026/8/28修正】fetch()に何も指定しないと、ブラウザ自身が持っているHTTPキャッシュ
  // （Service WorkerのCache APIとは別物）がまだ「新しい」と判断されている間は、
  // ネットワークに問い合わせすら行かずそのキャッシュがそのまま返ってきてしまう
  // ことが分かった。GitHub Pagesは配信するファイルに数分程度のキャッシュ有効期限を
  // 付けているため、更新後しばらくは（ブラウザを開き直しても）以前の内容が
  // 表示され続けてしまっていた。{cache: 'no-store'}を指定し、常にネットワークへ
  // 直接問い合わせて本当に最新の内容を取りに行くようにした。
  if (event.request.mode === 'navigate' || url.indexOf('index.html') !== -1) {
    event.respondWith(
      fetch(event.request, { cache: 'no-store' })
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => caches.match(event.request))
    );
    return;
  }

  // アイコンやmanifestなど、めったに変わらないファイルは「キャッシュがあればまずそれを返しつつ、裏で最新版に更新」する
  event.respondWith(
    caches.match(event.request).then((cached) => {
      const fetchPromise = fetch(event.request)
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => cached);
      return cached || fetchPromise;
    })
  );
});
）。古いキャッシュに透明版の
// アイコンが残っている端末でも、確実に新しい画像を取りに行けるようキャッシュ名を
// 更新した。
// 【2026/9/3追加】ホーム画面に追加したスマホ（特にiPhone）で、機材のカテゴリー分け
// 機能を追加したあとも古い画面のまま変わらないという報告があった。index.html自体は
// 毎回ネットワークから取りに行く設計（下のfetchハンドラ参照）なので通常は問題ない
// はずだが、機種によってはservice worker（このファイル自体）の更新チェックが
// すぐには走らず、古いservice workerが動き続けてしまうことがある。合言葉の
// 再ログインではこの層のキャッシュは消えない（ログイン情報とservice workerの
// キャッシュは別物のため）。キャッシュ名を上げておくことで、次に新しい
// service workerへの切り替えが行われたタイミングで、確実に前のキャッシュ
// （kizai-mobile-v7以前）が破棄されるようにした。
// 【2026/9/12追加】アプリ内「ガントチャート」タブを追加（index.html更新）。
// 念のため、こちらもキャッシュ名を上げておく。
// 【2026/9/13追加】ガントチャートタブが表示されない不具合の調査・修正のため
// index.htmlを更新。あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その2】ガントチャートの見た目を修正（機材名の欄を左右スクロール
// 対応にし、上部の凡例を機材カテゴリーのショートカットボタンに差し替え）。
// index.htmlを更新したため、あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その3】機材名欄の横スクロールを全行連動にした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加】機材名欄の連動スクロールが実機で効かない不具合を修正。
// ブラウザ標準の横スクロールに頼らず、指のドラッグを直接読み取る方式に作り替えた
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その2】機材名欄のスワイプが重いという指摘に対応。ドラッグ中に
// 毎回文字の幅を測り直していたのをやめ、描画時に1回だけ測ってキャッシュし、画面への
// 反映もrequestAnimationFrameでまとめるようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その3】機材名が短い行だけ途中で止まって見える動きは不要とのことで、
// 全行が常に同じ量だけ動くように変更（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/15追加】指が機材名欄からガントチャート本体側にはみ出すと途中でスライドが
// 止まってしまう不具合を修正。タッチ操作は生のTouchイベントで直接扱い、指の位置に
// 関わらず同じ指である限りスライドを継続するようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/16追加】機材名欄で指を上下に動かしたときは、ガントチャート全体を上下に
// 見られるよう、標準の縦スクロールに任せるようにした（横方向のスライドとどちらの
// 操作か、最初の指の動きで判定する）（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その2】カレンダーの日付が見づらいという指摘に対応し、数字を
// はっきり黒くした。ガントチャートの日付も、1つ1つ枠で囲んで見やすくした
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その3】日付の枠をヘッダーだけでなく、ガントチャート本体（予約の帯が
// 並ぶ部分）にも入れ、縦の区切り線でどの日付か分かりやすくした（index.html更新）。
// あわせてキャッシュ名も上げる。
const CACHE_NAME = 'kizai-mobile-v19';
const APP_SHELL = [
  './',
  './index.html',
  './manifest.json',
  './icon-192.png',
  './icon-512.png'
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => cache.addAll(APP_SHELL))
  );
  self.skipWaiting();
});

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((names) => {
      return Promise.all(
        names.filter((name) => name !== CACHE_NAME).map((name) => caches.delete(name))
      );
    })
  );
  self.clients.claim();
});

self.addEventListener('fetch', (event) => {
  const url = event.request.url;

  // Apps ScriptのWeb App（データの取得・保存）は常にネットワークを優先する
  if (url.indexOf('script.google.com') !== -1 || url.indexOf('script.googleusercontent.com') !== -1) {
    event.respondWith(
      fetch(event.request).catch(() => new Response(
        JSON.stringify({ ok: false, error: 'オフラインのため通信できません' }),
        { headers: { 'Content-Type': 'application/json' } }
      ))
    );
    return;
  }

  // ページ本体（index.html）は「まずネットワークから最新版を取りに行き、取れなければキャッシュ」にする
  // （こうしないと、コード更新後も古い画面がキャッシュから表示され続けてしまうため）
  // 【2026/8/28修正】fetch()に何も指定しないと、ブラウザ自身が持っているHTTPキャッシュ
  // （Service WorkerのCache APIとは別物）がまだ「新しい」と判断されている間は、
  // ネットワークに問い合わせすら行かずそのキャッシュがそのまま返ってきてしまう
  // ことが分かった。GitHub Pagesは配信するファイルに数分程度のキャッシュ有効期限を
  // 付けているため、更新後しばらくは（ブラウザを開き直しても）以前の内容が
  // 表示され続けてしまっていた。{cache: 'no-store'}を指定し、常にネットワークへ
  // 直接問い合わせて本当に最新の内容を取りに行くようにした。
  if (event.request.mode === 'navigate' || url.indexOf('index.html') !== -1) {
    event.respondWith(
      fetch(event.request, { cache: 'no-store' })
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => caches.match(event.request))
    );
    return;
  }

  // アイコンやmanifestなど、めったに変わらないファイルは「キャッシュがあればまずそれを返しつつ、裏で最新版に更新」する
  event.respondWith(
    caches.match(event.request).then((cached) => {
      const fetchPromise = fetch(event.request)
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => cached);
      return cached || fetchPromise;
    })
  );
});
部分を黒く塗りつぶして表示してしまうため、
// 「渡したアイコンと違って見える」原因になっていた）。古いキャッシュに透明版の
// アイコンが残っている端末でも、確実に新しい画像を取りに行けるようキャッシュ名を
// 更新した。
// 【2026/9/3追加】ホーム画面に追加したスマホ（特にiPhone）で、機材のカテゴリー分け
// 機能を追加したあとも古い画面のまま変わらないという報// 機材シート2026 モバイル用のサービスワーカー
// アプリの外枠（HTML/CSS/JS/アイコン）だけをキャッシュしておき、
// 電波が弱い場所でも起動できるようにする。// 機材シート2026 モバイル用のサービスワーカー
// アプリの外枠（HTML/CSS/JS/アイコン）だけをキャッシュしておき、
// 電波が弱い場所でも起動できるようにする。
// データそのもの（予約一覧など）は毎回ネットワークから取得する
// （キャッシュを見せてしまうと古いデータに気づけなくなるため）。

// 【2026/8/29修正】アイコン画像（icon-192.png/icon-512.png）を透明背景から白背景に
// 差し替えた（iOSのホーム画面アイコンは透明部分を黒く塗りつぶして表示してしまうため、
// 「渡したアイコンと違って見える」原因になっていた）。古いキャッシュに透明版の
// アイコンが残っている端末でも、確実に新しい画像を取りに行けるようキャッシュ名を
// 更新した。
// 【2026/9/3追加】ホーム画面に追加したスマホ（特にiPhone）で、機材のカテゴリー分け
// 機能を追加したあとも古い画面のまま変わらないという報告があった。index.html自体は
// 毎回ネットワークから取りに行く設計（下のfetchハンドラ参照）なので通常は問題ない
// はずだが、機種によってはservice worker（このファイル自体）の更新チェックが
// すぐには走らず、古いservice workerが動き続けてしまうことがある。合言葉の
// 再ログインではこの層のキャッシュは消えない（ログイン情報とservice workerの
// キャッシュは別物のため）。キャッシュ名を上げておくことで、次に新しい
// service workerへの切り替えが行われたタイミングで、確実に前のキャッシュ
// （kizai-mobile-v7以前）が破棄されるようにした。
// 【2026/9/12追加】アプリ内「ガントチャート」タブを追加（index.html更新）。
// 念のため、こちらもキャッシュ名を上げておく。
// 【2026/9/13追加】ガントチャートタブが表示されない不具合の調査・修正のため
// index.htmlを更新。あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その2】ガントチャートの見た目を修正（機材名の欄を左右スクロール
// 対応にし、上部の凡例を機材カテゴリーのショートカットボタンに差し替え）。
// index.htmlを更新したため、あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その3】機材名欄の横スクロールを全行連動にした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加】機材名欄の連動スクロールが実機で効かない不具合を修正。
// ブラウザ標準の横スクロールに頼らず、指のドラッグを直接読み取る方式に作り替えた
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その2】機材名欄のスワイプが重いという指摘に対応。ドラッグ中に
// 毎回文字の幅を測り直していたのをやめ、描画時に1回だけ測ってキャッシュし、画面への
// 反映もrequestAnimationFrameでまとめるようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その3】機材名が短い行だけ途中で止まって見える動きは不要とのことで、
// 全行が常に同じ量だけ動くように変更（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/15追加】指が機材名欄からガントチャート本体側にはみ出すと途中でスライドが
// 止まってしまう不具合を修正。タッチ操作は生のTouchイベントで直接扱い、指の位置に
// 関わらず同じ指である限りスライドを継続するようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/16追加】機材名欄で指を上下に動かしたときは、ガントチャート全体を上下に
// 見られるよう、標準の縦スクロールに任せるようにした（横方向のスライドとどちらの
// 操作か、最初の指の動きで判定する）（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/16追加 その2】カレンダーの日付が見づらいという指摘に対応し、数字を
// はっきり黒くした。ガントチャートの日付も、1つ1つ枠で囲んで見やすくした
// （index.html更新）。あわせてキャッシュ名も上げる。
const CACHE_NAME = 'kizai-mobile-v18';
const APP_SHELL = [
  './',
  './index.html',
  './manifest.json',
  './icon-192.png',
  './icon-512.png'
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => cache.addAll(APP_SHELL))
  );
  self.skipWaiting();
});

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((names) => {
      return Promise.all(
        names.filter((name) => name !== CACHE_NAME).map((name) => caches.delete(name))
      );
    })
  );
  self.clients.claim();
});

self.addEventListener('fetch', (event) => {
  const url = event.request.url;

  // Apps ScriptのWeb App（データの取得・保存）は常にネットワークを優先する
  if (url.indexOf('script.google.com') !== -1 || url.indexOf('script.googleusercontent.com') !== -1) {
    event.respondWith(
      fetch(event.request).catch(() => new Response(
        JSON.stringify({ ok: false, error: 'オフラインのため通信できません' }),
        { headers: { 'Content-Type': 'application/json' } }
      ))
    );
    return;
  }

  // ページ本体（index.html）は「まずネットワークから最新版を取りに行き、取れなければキャッシュ」にする
  // （こうしないと、コード更新後も古い画面がキャッシュから表示され続けてしまうため）
  // 【2026/8/28修正】fetch()に何も指定しないと、ブラウザ自身が持っているHTTPキャッシュ
  // （Service WorkerのCache APIとは別物）がまだ「新しい」と判断されている間は、
  // ネットワークに問い合わせすら行かずそのキャッシュがそのまま返ってきてしまう
  // ことが分かった。GitHub Pagesは配信するファイルに数分程度のキャッシュ有効期限を
  // 付けているため、更新後しばらくは（ブラウザを開き直しても）以前の内容が
  // 表示され続けてしまっていた。{cache: 'no-store'}を指定し、常にネットワークへ
  // 直接問い合わせて本当に最新の内容を取りに行くようにした。
  if (event.request.mode === 'navigate' || url.indexOf('index.html') !== -1) {
    event.respondWith(
      fetch(event.request, { cache: 'no-store' })
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => caches.match(event.request))
    );
    return;
  }

  // アイコンやmanifestなど、めったに変わらないファイルは「キャッシュがあればまずそれを返しつつ、裏で最新版に更新」する
  event.respondWith(
    caches.match(event.request).then((cached) => {
      const fetchPromise = fetch(event.request)
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => cached);
      return cached || fetchPromise;
    })
  );
});

// データそのもの（予約一覧など）は毎回ネットワークから取得する
// （キャッシュを見せてしまうと古いデータに気づけなくなるため）。

// 【2026/8/29修正】アイコン画像（icon-192.png/icon-512.png）を透明背景から白背景に
// 差し替えた（iOSのホーム画面アイコンは透明部分を黒く塗りつぶして表示してしまうため、
// 「渡したアイコンと違って見える」原因になっていた）。古いキャッシュに透明版の
// アイコンが残っている端末でも、確実に新しい画像を取りに行けるようキャッシュ名を
// 更新した。
// 【2026/9/3追加】ホーム画面に追加したスマホ（特にiPhone）で、機材のカテゴリー分け
// 機能を追加したあとも古い画面のまま変わらないという報告があった。index.html自体は
// 毎回ネットワークから取りに行く設計（下のfetchハンドラ参照）なので通常は問題ない
// はずだが、機種によってはservice worker（このファイル自体）の更新チェックが
// すぐには走らず、古いservice workerが動き続けてしまうことがある。合言葉の
// 再ログインではこの層のキャッシュは消えない（ログイン情報とservice workerの
// キャッシュは別物のため）。キャッシュ名を上げておくことで、次に新しい
// service workerへの切り替えが行われたタイミングで、確実に前のキャッシュ
// （kizai-mobile-v7以前）が破棄されるようにした。
// 【2026/9/12追加】アプリ内「ガントチャート」タブを追加（index.html更新）。
// 念のため、こちらもキャッシュ名を上げておく。
// 【2026/9/13追加】ガントチャートタブが表示されない不具合の調査・修正のため
// index.htmlを更新。あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その2】ガントチャートの見た目を修正（機材名の欄を左右スクロール
// 対応にし、上部の凡例を機材カテゴリーのショートカットボタンに差し替え）。
// index.htmlを更新したため、あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その3】機材名欄の横スクロールを全行連動にした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加】機材名欄の連動スクロールが実機で効かない不具合を修正。
// ブラウザ標準の横スクロールに頼らず、指のドラッグを直接読み取る方式に作り替えた
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その2】機材名欄のスワイプが重いという指摘に対応。ドラッグ中に
// 毎回文字の幅を測り直していたのをやめ、描画時に1回だけ測ってキャッシュし、画面への
// 反映もrequestAnimationFrameでまとめるようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その3】機材名が短い行だけ途中で止まって見える動きは不要とのことで、
// 全行が常に同じ量だけ動くように変更（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/15追加】指が機材名欄からガントチャート本体側にはみ出すと途中でスライドが
// 止まってしまう不具合を修正。タッチ操作は生のTouchイベントで直接扱い、指の位置に
// 関わらず同じ指である限りスライドを継続するようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/16追加】機材名欄で指を上下に動かしたときは、ガントチャート全体を上下に
// 見られるよう、標準の縦スクロールに任せるようにした（横方向のスライドとどちらの
// 操作か、最初の指の動きで判定する）（index.html更新）。あわせてキャッシュ名も上げる。
const CACHE_NAME = 'kizai-mobile-v17';
const APP_SHELL = [
  './',
  './index.html',
  './manifest.json',
  './icon-192.png',
  './icon-512.png'
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => cache.addAll(APP_SHELL))
  );
  self.skipWaiting();
});

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((names) => {
      return Promise.all(
        names.filter((name) => name !== CACHE_NAME).map((name) => caches.delete(name))
      );
    })
  );
  self.clients.claim();
});

self.addEventListener('fetch', (event) => {
  const url = event.request.url;

  // Apps ScriptのWeb App（データの取得・保存）は常にネットワークを優先する
  if (url.indexOf('script.google.com') !== -1 || url.indexOf('script.googleusercontent.com') !== -1) {
    event.respondWith(
      fetch(event.request).catch(() => new Response(
        JSON.stringify({ ok: false, error: 'オフラインのため通信できません' }),
        { headers: { 'Content-Type': 'application/json' } }
      ))
    );
    return;
  }

  // ページ本体（index.html）は「まずネットワークから最新版を取りに行き、取れなければキャッシュ」にする
  // （こうしないと、コード更新後も古い画面がキャッシュから表示され続けてしまうため）
  // 【2026/8/28修正】fetch()に何も指定しないと、ブラウザ自身が持っているHTTPキャッシュ
  // （Service WorkerのCache APIとは別物）がまだ「新しい」と判断されている間は、
  // ネットワークに問い合わせすら行かずそのキャッシュがそのまま返ってきてしまう
  // ことが分かった。GitHub Pagesは配信するファイルに数分程度のキャッシュ有効期限を
  // 付けているため、更新後しばらくは（ブラウザを開き直しても）以前の内容が
  // 表示され続けてしまっていた。{cache: 'no-store'}を指定し、常にネットワークへ
  // 直接問い合わせて本当に最新の内容を取りに行くようにした。
  if (event.request.mode === 'navigate' || url.indexOf('index.html') !== -1) {
    event.respondWith(
      fetch(event.request, { cache: 'no-store' })
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => caches.match(event.request))
    );
    return;
  }

  // アイコンやmanifestなど、めったに変わらないファイルは「キャッシュがあればまずそれを返しつつ、裏で最新版に更新」する
  event.respondWith(
    caches.match(event.request).then((cached) => {
      const fetchPromise = fetch(event.request)
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => cached);
      return cached || fetchPromise;
    })
  );
});
告があった。index.html自体は
// 毎回ネットワークから取りに行く設計（下のfetchハンドラ参照）なので通常は問題ない
// はずだが、機種によってはservice worke// 機材シート2026 モバイル用のサービスワーカー
// アプリの外枠（HTML/CSS/JS/アイコン）だけをキャッシュしておき、
// 電波が弱い場所でも起動できるようにする。
// データそのもの（予約一覧など）は毎回ネットワークから取得する
// （キャッシュを見せてしまうと古いデータに気づけなくなるため）。

// 【2026/8/29修正】アイコン画像（icon-192.png/icon-512.png）を透明背景から白背景に
// 差し替えた（iOSのホーム画面アイコンは透明部分を黒く塗りつぶして表示してしまうため、
// 「渡したアイコンと違って見える」原因になっていた）。古いキャッシュに透明版の
// アイコンが残っている端末でも、確実に新しい画像を取りに行けるようキャッシュ名を
// 更新した。
// 【2026/9/3追加】ホーム画面に追加したスマホ（特にiPhone）で、機材のカテゴリー分け
// 機能を追加したあとも古い画面のまま変わらないという報告があった。index.html自体は
// 毎回ネットワークから取りに行く設計（下のfetchハンドラ参照）なので通常は問題ない
// はずだが、機種によってはservice worker（このファイル自体）の更新チェックが
// すぐには走らず、古いservice workerが動き続けてしまうことがある。合言葉の
// 再ログインではこの層のキャッシュは消えない（ログイン情報とservice workerの
// キャッシュは別物のため）。キャッシュ名を上げておくことで、次に新しい
// service workerへの切り替えが行われたタイミングで、確実に前のキャッシュ
// （kizai-mobile-v7以前）が破棄されるようにした。
// 【2026/9/12追加】アプリ内「ガントチャート」タブを追加（index.html更新）。
// 念のため、こちらもキャッシュ名を上げておく。
// 【2026/9/13追加】ガントチャートタブが表示されない不具合の調査・修正のため
// index.htmlを更新。あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その2】ガントチャートの見た目を修正（機材名の欄を左右スクロール
// 対応にし、上部の凡例を機材カテゴリーのショートカットボタンに差し替え）。
// index.htmlを更新したため、あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その3】機材名欄の横スクロールを全行連動にした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加】機材名欄の連動スクロールが実機で効かない不具合を修正。
// ブラウザ標準の横スクロールに頼らず、指のドラッグを直接読み取る方式に作り替えた
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その2】機材名欄のスワイプが重いという指摘に対応。ドラッグ中に
// 毎回文字の幅を測り直していたのをやめ、描画時に1回だけ測ってキャッシュし、画面への
// 反映もrequestAnimationFrameでまとめるようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その3】機材名が短い行だけ途中で止まって見える動きは不要とのことで、
// 全行が常に同じ量だけ動くように変更（index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/15追加】指が機材名欄からガントチャート本体側にはみ出すと途中でスライドが
// 止まってしまう不具合を修正。タッチ操作は生のTouchイベントで直接扱い、指の位置に
// 関わらず同じ指である限りスライドを継続するようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
const CACHE_NAME = 'kizai-mobile-v16';
const APP_SHELL = [
  './',
  './index.html',
  './manifest.json',
  './icon-192.png',
  './icon-512.png'
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => cache.addAll(APP_SHELL))
  );
  self.skipWaiting();
});

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((names) => {
      return Promise.all(
        names.filter((name) => name !== CACHE_NAME).map((name) => caches.delete(name))
      );
    })
  );
  self.clients.claim();
});

self.addEventListener('fetch', (event) => {
  const url = event.request.url;

  // Apps ScriptのWeb App（データの取得・保存）は常にネットワークを優先する
  if (url.indexOf('script.google.com') !== -1 || url.indexOf('script.googleusercontent.com') !== -1) {
    event.respondWith(
      fetch(event.request).catch(() => new Response(
        JSON.stringify({ ok: false, error: 'オフラインのため通信できません' }),
        { headers: { 'Content-Type': 'application/json' } }
      ))
    );
    return;
  }

  // ページ本体（index.html）は「まずネットワークから最新版を取りに行き、取れなければキャッシュ」にする
  // （こうしないと、コード更新後も古い画面がキャッシュから表示され続けてしまうため）
  // 【2026/8/28修正】fetch()に何も指定しないと、ブラウザ自身が持っているHTTPキャッシュ
  // （Service WorkerのCache APIとは別物）がまだ「新しい」と判断されている間は、
  // ネットワークに問い合わせすら行かずそのキャッシュがそのまま返ってきてしまう
  // ことが分かった。GitHub Pagesは配信するファイルに数分程度のキャッシュ有効期限を
  // 付けているため、更新後しばらくは（ブラウザを開き直しても）以前の内容が
  // 表示され続けてしまっていた。{cache: 'no-store'}を指定し、常にネットワークへ
  // 直接問い合わせて本当に最新の内容を取りに行くようにした。
  if (event.request.mode === 'navigate' || url.indexOf('index.html') !== -1) {
    event.respondWith(
      fetch(event.request, { cache: 'no-store' })
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => caches.match(event.request))
    );
    return;
  }

  // アイコンやmanifestなど、めったに変わらないファイルは「キャッシュがあればまずそれを返しつつ、裏で最新版に更新」する
  event.respondWith(
    caches.match(event.request).then((cached) => {
      const fetchPromise = fetch(event.request)
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => cached);
      return cached || fetchPromise;
    })
  );
});
r（このファイル自体）の更新チェックが
// すぐには走らず、古いservice workerが動き続けてしまうことがある。合言葉の
// 再ログインではこの層のキャッシュは消えない（ログイン情報とservice workerの
// キャッシュは別物のため）。キャッシュ名を上げておくことで、次に新しい
// service workerへの切り替えが行われたタイミングで、確実に前のキャッシュ
// （kizai-mobile-v7以前）が破棄されるようにした。
// 【2026/9/12追加】アプリ内「ガントチャート」タブを追加（index.html更新）。
// 念のため、こちらもキャッシュ名を上げておく。
// 【2026/9/13追加】ガントチャートタブが表示されない不具合の調査・修正のため
// index.htmlを更新。あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その2】ガントチャートの見た目を修正（機材名の欄を左右スクロール
// 対応にし、上部の凡例を機材カテゴリーのショートカットボタンに差し替え）。
// index.htmlを更新したため、あわせてキャッシュ名も上げる。
// 【2026/9/13追加 その3】機材名欄の横スクロールを全行連動にした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加】機材名欄の連動スクロールが実機で効かない不具合を修正。
// ブラウザ標準の横スクロールに頼らず、指のドラッグを直接読み取る方式に作り替えた
// （index.html更新）。あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その2】機材名欄のスワイプが重いという指摘に対応。ドラッグ中に
// 毎回文字の幅を測り直していたのをやめ、描画時に1回だけ測ってキャッシュし、画面への
// 反映もrequestAnimationFrameでまとめるようにした（index.html更新）。
// あわせてキャッシュ名も上げる。
// 【2026/9/14追加 その3】機材名が短い行だけ途中で止まって見える動きは不要とのことで、
// 全行が常に同じ量だけ動くように変更（index.html更新）。あわせてキャッシュ名も上げる。
const CACHE_NAME = 'kizai-mobile-v15';
const APP_SHELL = [
  './',
  './index.html',
  './manifest.json',
  './icon-192.png',
  './icon-512.png'
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => cache.addAll(APP_SHELL))
  );
  self.skipWaiting();
});

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((names) => {
      return Promise.all(
        names.filter((name) => name !== CACHE_NAME).map((name) => caches.delete(name))
      );
    })
  );
  self.clients.claim();
});

self.addEventListener('fetch', (event) => {
  const url = event.request.url;

  // Apps ScriptのWeb App（データの取得・保存）は常にネットワークを優先する
  if (url.indexOf('script.google.com') !== -1 || url.indexOf('script.googleusercontent.com') !== -1) {
    event.respondWith(
      fetch(event.request).catch(() => new Response(
        JSON.stringify({ ok: false, error: 'オフラインのため通信できません' }),
        { headers: { 'Content-Type': 'application/json' } }
      ))
    );
    return;
  }

  // ページ本体（index.html）は「まずネットワークから最新版を取りに行き、取れなければキャッシュ」にする
  // （こうしないと、コード更新後も古い画面がキャッシュから表示され続けてしまうため）
  // 【2026/8/28修正】fetch()に何も指定しないと、ブラウザ自身が持っているHTTPキャッシュ
  // （Service WorkerのCache APIとは別物）がまだ「新しい」と判断されている間は、
  // ネットワークに問い合わせすら行かずそのキャッシュがそのまま返ってきてしまう
  // ことが分かった。GitHub Pagesは配信するファイルに数分程度のキャッシュ有効期限を
  // 付けているため、更新後しばらくは（ブラウザを開き直しても）以前の内容が
  // 表示され続けてしまっていた。{cache: 'no-store'}を指定し、常にネットワークへ
  // 直接問い合わせて本当に最新の内容を取りに行くようにした。
  if (event.request.mode === 'navigate' || url.indexOf('index.html') !== -1) {
    event.respondWith(
      fetch(event.request, { cache: 'no-store' })
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => caches.match(event.request))
    );
    return;
  }

  // アイコンやmanifestなど、めったに変わらないファイルは「キャッシュがあればまずそれを返しつつ、裏で最新版に更新」する
  event.respondWith(
    caches.match(event.request).then((cached) => {
      const fetchPromise = fetch(event.request)
        .then((response) => {
          if (response && response.status === 200) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
          return response;
        })
        .catch(() => cached);
      return cached || fetchPromise;
    })
  );
});
